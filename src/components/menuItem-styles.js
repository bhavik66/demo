import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    marginTop: 16,
    alignItems: 'center',
  },
  image: {
    height: 64,
    width: 64,
    borderRadius: 40,
    marginRight: 16,
  },
});

export default styles;
